syntax = "proto3";
package posts.v1;

import "google/protobuf/empty.proto";

service PostsService {
  rpc Posts   (PostsRequest)   returns (PostsResponse);
}

message PostsRequest {
  string text_channel_id = 1;
  int64 limit = 2;
  optional string starting_before_timestamp = 3;
}
